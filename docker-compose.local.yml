version: '3.8'

services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: tuki_local
      POSTGRES_USER: tuki_user
      POSTGRES_PASSWORD: tuki_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  backend:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - DB_NAME=tuki_local
      - DB_USER=tuki_user
      - DB_PASSWORD=tuki_password
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - SECRET_KEY=local-dev-secret-key
      - ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.156,0.0.0.0
      - CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://192.168.1.156:8000,https://192.168.1.156:3000
      # 🚀 ENTERPRISE: Google Cloud Storage Configuration
      # Set to False to use local storage in development (avoids credential issues)
      # Set to True to use GCS (requires valid credentials)
      - USE_GCS_IN_DEV=False
      - GS_BUCKET_NAME=tuki-media-prod-1759240560
      - GS_PROJECT_ID=tukiprod
      - GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-credentials.json
      # 🚀 ENTERPRISE: WooCommerce Sync Configuration
      - WOOCOMMERCE_SSH_HOST=${WOOCOMMERCE_SSH_HOST}
      - WOOCOMMERCE_SSH_PORT=${WOOCOMMERCE_SSH_PORT}
      - WOOCOMMERCE_SSH_USERNAME=${WOOCOMMERCE_SSH_USERNAME}
      - WOOCOMMERCE_SSH_TIMEOUT=${WOOCOMMERCE_SSH_TIMEOUT}
      - WOOCOMMERCE_SSH_MAX_RETRIES=${WOOCOMMERCE_SSH_MAX_RETRIES}
      - WOOCOMMERCE_SSH_RETRY_DELAY=${WOOCOMMERCE_SSH_RETRY_DELAY}
      - WOOCOMMERCE_SSH_PRIVATE_KEY_PATH=${WOOCOMMERCE_SSH_PRIVATE_KEY_PATH}
      - WOOCOMMERCE_SSH_PRIVATE_KEY_PASSPHRASE=${WOOCOMMERCE_SSH_PRIVATE_KEY_PASSPHRASE}
      - WOOCOMMERCE_MYSQL_HOST=${WOOCOMMERCE_MYSQL_HOST}
      - WOOCOMMERCE_MYSQL_PORT=${WOOCOMMERCE_MYSQL_PORT}
      - WOOCOMMERCE_MYSQL_DATABASE=${WOOCOMMERCE_MYSQL_DATABASE}
      - WOOCOMMERCE_MYSQL_USERNAME=${WOOCOMMERCE_MYSQL_USERNAME}
      - WOOCOMMERCE_MYSQL_PASSWORD=${WOOCOMMERCE_MYSQL_PASSWORD}
      - WOOCOMMERCE_MYSQL_CHARSET=${WOOCOMMERCE_MYSQL_CHARSET}
      - WOOCOMMERCE_SYNC_ENABLED=${WOOCOMMERCE_SYNC_ENABLED}
      - WOOCOMMERCE_SYNC_DEFAULT_SERVICE_FEE=${WOOCOMMERCE_SYNC_DEFAULT_SERVICE_FEE}
      - WOOCOMMERCE_SYNC_MAX_RETRIES=${WOOCOMMERCE_SYNC_MAX_RETRIES}
      - WOOCOMMERCE_SYNC_RETRY_DELAY=${WOOCOMMERCE_SYNC_RETRY_DELAY}
    depends_on:
      - db
      - redis
    volumes:
      - .:/app
      - /app/staticfiles
      - /app/media
      # 🚀 ENTERPRISE: Mount Google Cloud credentials
      - ./gcp-credentials.json:/app/gcp-credentials.json:ro

  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile.local
    command: celery -A config worker -l info -Q sync-heavy,default,emails,critical,maintenance,documents
    environment:
      - DEBUG=True
      - DB_NAME=tuki_local
      - DB_USER=tuki_user
      - DB_PASSWORD=tuki_password
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - SECRET_KEY=local-dev-secret-key
      # 🚀 ENTERPRISE: WooCommerce Sync Configuration
      - WOOCOMMERCE_SSH_HOST=${WOOCOMMERCE_SSH_HOST}
      - WOOCOMMERCE_SSH_PORT=${WOOCOMMERCE_SSH_PORT}
      - WOOCOMMERCE_SSH_USERNAME=${WOOCOMMERCE_SSH_USERNAME}
      - WOOCOMMERCE_SSH_TIMEOUT=${WOOCOMMERCE_SSH_TIMEOUT}
      - WOOCOMMERCE_SSH_MAX_RETRIES=${WOOCOMMERCE_SSH_MAX_RETRIES}
      - WOOCOMMERCE_SSH_RETRY_DELAY=${WOOCOMMERCE_SSH_RETRY_DELAY}
      - WOOCOMMERCE_SSH_PRIVATE_KEY_PATH=${WOOCOMMERCE_SSH_PRIVATE_KEY_PATH}
      - WOOCOMMERCE_SSH_PRIVATE_KEY_PASSPHRASE=${WOOCOMMERCE_SSH_PRIVATE_KEY_PASSPHRASE}
      - WOOCOMMERCE_MYSQL_HOST=${WOOCOMMERCE_MYSQL_HOST}
      - WOOCOMMERCE_MYSQL_PORT=${WOOCOMMERCE_MYSQL_PORT}
      - WOOCOMMERCE_MYSQL_DATABASE=${WOOCOMMERCE_MYSQL_DATABASE}
      - WOOCOMMERCE_MYSQL_USERNAME=${WOOCOMMERCE_MYSQL_USERNAME}
      - WOOCOMMERCE_MYSQL_PASSWORD=${WOOCOMMERCE_MYSQL_PASSWORD}
      - WOOCOMMERCE_MYSQL_CHARSET=${WOOCOMMERCE_MYSQL_CHARSET}
      - WOOCOMMERCE_SYNC_ENABLED=${WOOCOMMERCE_SYNC_ENABLED}
      - WOOCOMMERCE_SYNC_DEFAULT_SERVICE_FEE=${WOOCOMMERCE_SYNC_DEFAULT_SERVICE_FEE}
      - WOOCOMMERCE_SYNC_MAX_RETRIES=${WOOCOMMERCE_SYNC_MAX_RETRIES}
      - WOOCOMMERCE_SYNC_RETRY_DELAY=${WOOCOMMERCE_SYNC_RETRY_DELAY}
    depends_on:
      - db
      - redis
    volumes:
      - .:/app

  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile.local
    command: celery -A config beat -l info
    environment:
      - DEBUG=True
      - DB_NAME=tuki_local
      - DB_USER=tuki_user
      - DB_PASSWORD=tuki_password
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - SECRET_KEY=local-dev-secret-key
      # 🚀 ENTERPRISE: WooCommerce Sync Configuration
      - WOOCOMMERCE_SSH_HOST=${WOOCOMMERCE_SSH_HOST}
      - WOOCOMMERCE_SSH_PORT=${WOOCOMMERCE_SSH_PORT}
      - WOOCOMMERCE_SSH_USERNAME=${WOOCOMMERCE_SSH_USERNAME}
      - WOOCOMMERCE_SSH_TIMEOUT=${WOOCOMMERCE_SSH_TIMEOUT}
      - WOOCOMMERCE_SSH_MAX_RETRIES=${WOOCOMMERCE_SSH_MAX_RETRIES}
      - WOOCOMMERCE_SSH_RETRY_DELAY=${WOOCOMMERCE_SSH_RETRY_DELAY}
      - WOOCOMMERCE_SSH_PRIVATE_KEY_PATH=${WOOCOMMERCE_SSH_PRIVATE_KEY_PATH}
      - WOOCOMMERCE_SSH_PRIVATE_KEY_PASSPHRASE=${WOOCOMMERCE_SSH_PRIVATE_KEY_PASSPHRASE}
      - WOOCOMMERCE_MYSQL_HOST=${WOOCOMMERCE_MYSQL_HOST}
      - WOOCOMMERCE_MYSQL_PORT=${WOOCOMMERCE_MYSQL_PORT}
      - WOOCOMMERCE_MYSQL_DATABASE=${WOOCOMMERCE_MYSQL_DATABASE}
      - WOOCOMMERCE_MYSQL_USERNAME=${WOOCOMMERCE_MYSQL_USERNAME}
      - WOOCOMMERCE_MYSQL_PASSWORD=${WOOCOMMERCE_MYSQL_PASSWORD}
      - WOOCOMMERCE_MYSQL_CHARSET=${WOOCOMMERCE_MYSQL_CHARSET}
      - WOOCOMMERCE_SYNC_ENABLED=${WOOCOMMERCE_SYNC_ENABLED}
      - WOOCOMMERCE_SYNC_DEFAULT_SERVICE_FEE=${WOOCOMMERCE_SYNC_DEFAULT_SERVICE_FEE}
      - WOOCOMMERCE_SYNC_MAX_RETRIES=${WOOCOMMERCE_SYNC_MAX_RETRIES}
      - WOOCOMMERCE_SYNC_RETRY_DELAY=${WOOCOMMERCE_SYNC_RETRY_DELAY}
    depends_on:
      - db
      - redis
    volumes:
      - .:/app

volumes:
  postgres_data:
