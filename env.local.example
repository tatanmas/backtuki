# 🚀 ENTERPRISE LOCAL DEVELOPMENT WITH GOOGLE CLOUD STORAGE
# Copy this file to .env and fill in the values

# Django Settings
DJANGO_SETTINGS_MODULE=config.settings.development
DEBUG=True

# Database
DB_NAME=tuki_db
DB_USER=tuki_user
DB_PASSWORD=tuki_password
DB_HOST=db
DB_PORT=5432

# Google Cloud Storage Configuration
USE_GCS_IN_DEV=True
GS_BUCKET_NAME=tuki-media-prod-1759240560
GS_PROJECT_ID=tukiprod

# 🚀 ENTERPRISE: Service Account Credentials (JSON format)
# Get this from Google Cloud Console > IAM & Admin > Service Accounts
# Create a service account with Storage Admin role and download the JSON key
# Then paste the entire JSON content as a single line string
GS_CREDENTIALS={"type":"service_account","project_id":"your-project-id",...}

# Frontend URL
FRONTEND_URL=http://localhost:8080

# Email Configuration
EMAIL_HOST_USER=noreply@tuki.cl
EMAIL_HOST_PASSWORD=-W7)HsC<Hsfk

# Security
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# JWT
JWT_SECRET_KEY=your-jwt-secret-key-here
